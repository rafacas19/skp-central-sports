# Web framework — FastAPI app owns the process and the Telegram webhook.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35

# Telegram bot framework.
# [job-queue] pulls in APScheduler, required for the auto-nudge job.
# (No [webhooks] extra: FastAPI handles HTTP; PTB only processes updates.)
python-telegram-bot[job-queue]==21.6

# ORM + migrations + async Postgres driver.
tortoise-orm[asyncpg]>=0.23,<0.24
aerich>=0.8,<0.9

# Configuration
python-dotenv==1.0.1

# Excel report export (.xlsx workbooks). Pure-Python, no system deps.
openpyxl>=3.1,<4.0

# AI providers (only needed when USE_MOCK_AI=false)
# Use floors, not exact pins: older anthropic/openai releases pass a `proxies`
# kwarg that httpx>=0.28 removed, which crashes at client construction.
anthropic>=0.69,<1.0
openai>=2.0,<3.0
httpx>=0.28,<1.0

# Testing — tests run against a real Postgres (same engine as production),
# provided locally via docker-compose (the `db` service).
pytest==8.3.3
pytest-asyncio==0.24.0
