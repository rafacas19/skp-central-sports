# Core Telegram bot framework
# [webhooks] pulls in tornado, required for run_webhook (production mode).
# [job-queue] pulls in APScheduler, required for the auto-nudge job.
python-telegram-bot[webhooks,job-queue]==21.6

# Configuration
python-dotenv==1.0.1

# Database (Postgres) — psycopg 3 with a connection pool.
# [binary] ships prebuilt libpq wheels so no system Postgres is needed to build.
psycopg[binary,pool]>=3.2,<4.0

# AI providers (only needed when USE_MOCK_AI=false)
# Use floors, not exact pins: older anthropic/openai releases pass a `proxies`
# kwarg that httpx>=0.28 removed, which crashes at client construction.
anthropic>=0.69,<1.0
openai>=2.0,<3.0
httpx>=0.28,<1.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
